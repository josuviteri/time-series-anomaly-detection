Deep learning project that uses a LSTM + autoencoder model to detect anomalies in a time series dataset that tracks the value of a currency. 
The workflow tracks the entire process, from data preprocessing, different model architecture comparisons and model training and evaluation.
